import React from 'react'
import ReactDOM from 'react-dom/client'
import ConVoiceApp from './ConVoiceApp.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ConVoiceApp />
    </React.StrictMode>,
)